<!DOCTYPE html>
<html class="h-full" lang="fr">
  <head>
    <title><%= content_for(:title) || "Entrelacs" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
    <meta name="theme-color" content="#0e7c7c">
    <meta name="application-name" content="Entrelacs">
    <meta name="description" content="Gestion d'habitat partagé pour les tisseurs">

    <!-- PWA iOS -->
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <meta name="apple-mobile-web-app-title" content="Entrelacs">

    <!-- PWA Android -->
    <meta name="mobile-web-app-capable" content="yes">
    <link rel="manifest" href="/manifest.json">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Harmattan:wght@400;500;600;700&display=swap" rel="stylesheet">

    <!-- Icons -->
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icons/icon-192.png">
    <link rel="apple-touch-icon" sizes="152x152" href="/icons/icon-152.png">
    <link rel="apple-touch-icon" sizes="192x192" href="/icons/icon-192.png">
    <link rel="apple-touch-icon" sizes="512x512" href="/icons/icon-512.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="h-full bg-tisseurs-cream font-harmattan" data-controller="service-worker">
    <a href="#main-content" class="sr-only focus:not-sr-only focus:absolute focus:top-2 focus:left-2 focus:z-50 focus:rounded-md focus:bg-tisseurs-teal focus:px-4 focus:py-2 focus:text-white focus:outline-none">
      Aller au contenu principal
    </a>
    <% if user_signed_in? %>
      <div class="min-h-full">
        <%= render "shared/navbar" %>

        <div class="py-10">
          <% if content_for?(:page_header) %>
            <header>
              <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
                <%= yield :page_header %>
              </div>
            </header>
          <% end %>

          <main id="main-content">
            <div class="mx-auto max-w-7xl px-4 py-8 sm:px-6 lg:px-8">
              <%= yield %>
            </div>
          </main>
        </div>
      </div>
    <% else %>
      <main id="main-content">
        <%= yield %>
      </main>
    <% end %>

    <%= render "shared/flash" %>
  </body>
</html>
