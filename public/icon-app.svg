<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512">
  <!-- Background -->
  <rect width="512" height="512" rx="96" fill="#0e7c7c"/>

  <!-- Stylized T -->
  <g fill="#ffffff">
    <!-- Horizontal bar of T -->
    <rect x="96" y="120" width="320" height="48" rx="8"/>

    <!-- Vertical bar of T -->
    <rect x="232" y="120" width="48" height="272" rx="8"/>
  </g>

  <!-- Decorative wave (like the "les" underline) -->
  <path d="M96 420 Q160 380 256 400 Q352 420 416 380"
        stroke="#f26851"
        stroke-width="24"
        stroke-linecap="round"
        fill="none"/>
</svg>
